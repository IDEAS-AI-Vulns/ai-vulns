   server {
       listen 80;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }

        error_page 400 /400-test.html;

        location = /400-test.html {
                root /usr/share/nginx/html;
                internal;
            }
    }
